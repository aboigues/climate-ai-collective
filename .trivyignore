# Trivy Ignore File
# Ce fichier permet d'ignorer certaines vulnérabilités spécifiques
# Format: CVE-YYYY-XXXXX ou GHSA-xxxx-xxxx-xxxx
#
# Exemple d'utilisation:
# CVE-2023-12345  # Ignorer cette CVE car elle ne s'applique pas à notre cas d'usage
# GHSA-xxxx-xxxx-xxxx  # Faux positif confirmé par l'équipe sécurité
#
# IMPORTANT: Chaque ligne ignorée doit avoir un commentaire expliquant pourquoi
# et doit être revue régulièrement (idéalement chaque trimestre)
#
# Dernière révision: 2025-10-31

# Ajoutez ici les CVEs à ignorer avec leurs justifications
# Exemple:
# CVE-2024-12345  # Ne s'applique pas car nous n'utilisons pas la fonctionnalité vulnérable

# CVE-2026-0861 - glibc: Integer overflow in memalign leads to heap corruption
# Raison: Pas de correctif disponible dans Debian 13.3 (Trixie) au moment de ce commit (2026-01-22)
# Affecte: libc-bin et libc6 version 2.41-12+deb13u1
# Image de base: python:3.11-slim utilise Debian Trixie
# À revoir: Lorsqu'un patch sera disponible dans les mises à jour de sécurité Debian
# Status: affected (pas de version fixée disponible)
CVE-2026-0861
